<extend name="Public:base"/>
<block name="content">
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    评论管理
                </div>
                <div class="panel-heading">
                    <div class="row">
                        <div class="col-lg-12">
                            <form action="{:U('Comment/index')}" class="form-inline" onsubmit="return checkForm();">
                                <div class="form-group">
                                    <label for="">用户昵称</label>
                                    <input type="text" name="nickname" class="form-control" value="{:I('nickname')}">
                                </div>
                                <div class="form-group">
                                    <label for="">评论内容</label>
                                    <input type="text" name="content" class="form-control" value="{:I('content')}">
                                </div>
                                <div class="form-group">
                                    <label for="">时间</label>
                                    <input type="date" name="sdate" class="inline form-control" id="sdate"
                                           value="{:I('sdate')}" min="2017-01-01" max="{:date('Y-m-d')}">
                                    --
                                    <input type="date" name="edate" class="inline form-control"
                                           id="edate" value="{:I('edate')}" min="2017-01-01" max="{:date('Y-m-d')}">
                                </div>

                                <button class="btn btn-default form-control" type="submit">查看</button>
                            </form>
                        </div><!-- /.col-lg-6 -->
                    </div><!-- /.row -->
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover">
                        <thead>
                        <tr>
                            <th></th>
                            <th>评论内容</th>
                            <th>用户ID</th>
                            <th>用户当前昵称</th>


                            <th>评论时间</th>
                            <th>操作</th>

                        </tr>
                        </thead>
                        <tbody>
                        <form name="commentsform" id="commentsform" action="{:U('Comment/delecomment')}"
                              class="form-inline">
                            <foreach name="lists" item="comment" key="k">
                                <tr>
                                    <td><input name="id[]" type="checkbox" value="{$comment['id']}"/></td>
                                    <td><?php
                                        echo '<strong style="color: #0000cc">' . htmlspecialchars($artwork_update[$comment['id']]['artwork']['name']);
                                        if ($artwork_update[$comment['id']]['artupdate']['number']) echo '- <span style="color: #017ebc">更新(' . $artwork_update[$comment['id']]['artupdate']['number'] . ').</span>';
                                        echo '</strong>';
                                        ?>
                                        <div style="border:1px dashed #C0C0C0; padding: 2px; margin-top: 5px;">
                                            <?php echo htmlspecialchars($comment['nickname']) ?>：<?php

                                            if ($comment['flag'] == 2) {
                                                echo htmlspecialchars(base64_decode($comment['content']));

                                            } else {
                                                echo htmlspecialchars($comment['content']);
                                            }


                                            ?></div>
                                    </td>
                                    <td>{$comment['commenter']}</td>
                                    <td><?php echo htmlspecialchars($Userlist[$comment['commenter']]['nickname']) ?></td>


                                    <td><?php echo date('Y-m-d H:i:s', $comment['create_time']) ?></td>
                                    <td><a href="#" onclick="return delete_comment(<?php echo $comment['id']; ?>,this)">
                                            删除</a></td>
                                </tr>
                            </foreach>
                            <tr>
                                <th colspan="6"><a href="#" onclick="return check_all()">全选</a>&nbsp;/<a href="#"
                                                                                                         onclick="return dis_check_all()">全不选</a>&nbsp;/<a
                                        href="#" onclick="return check_fan()">反选</a> &nbsp;||&nbsp;&nbsp;&nbsp;<a
                                        href="#" onclick="return dele_select(this)">删除选中的评论</a></th>


                            </tr>
                        </form>
                        </tbody>
                    </table>
                    <!-- /.table-responsive -->
                    {$nav}
                    <!-- TODO:page -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
</block>
<block name="customJs">
    <script>
      /*  $(document).ready(function () {

            $('#az-id-date-selector').datetimepicker({
                format: 'yyyy-mm-dd',
                language: 'zh-CN',
                weekStart: 1,
                todayBtn: 1,
                autoclose: 1,
                todayHighlight: 1,
                startView: 2,
                minView: 2,
                forceParse: 0
            });
            $('#az-id-date-selector2').datetimepicker({
                format: 'yyyy-mm-dd',
                language: 'zh-CN',
                weekStart: 1,
                todayBtn: 1,
                autoclose: 1,
                todayHighlight: 1,
                startView: 2,
                minView: 2,
                forceParse: 0
            })


        });*/

      function checkForm(){
          var sdate = $("#sdate").val();
          var edate = $("#edate").val();
          var check = true;
          var message = "";
          if(sdate!='' && edate!='' && sdate>edate){
              var check = false;
              message = message + "<li>开始时间不能晚于结束时间!</li>";
          }
          if (check == false) {
              $.prompt("<span style=\" color:#AD6C14\">"+message+"</span>", {
                  title: "提示",
                  buttons: {"确定": false}
              });
              return false;
          } else {
              return true;
          }

      }

    </script>
    <script type="text/javascript">
        function delete_comment(id, obj) {
            if (confirm("确定删除么?")) {
                $(obj).after("<img id='loading" + id + "' src='/Public/Admin/image/loading.gif' /> ");
                $.ajax({
                    type: 'post',
                    url: '<?php echo U('Comment/delecomment');?>',
                    data: "id=" + id,
                    cache: false,
                    dataType: 'json',
                    error: function (data) {
                        alert("error");
                    },
                    success: function (data) {
                        // alert(data);
                        if (data.error == 0) {
                            $("#loading" + id).remove();
                            $(obj).parent().parent().remove();
                        }

                    }
                });
            }
            return false;
        }
        function check_all() {

            $('input[name="id[]"]').prop("checked", true);
        }

        function dis_check_all() {
            $('input[name="id[]"]').prop("checked", false);
        }

        function check_fan() {
            $('input[name="id[]"]').each(function () {
                $(this).prop("checked", function (index, attr) {
                    return !attr;
                });
            });
        }
        function dele_select(obj) {

            if (confirm("确定删除么?")) {
                $(obj).after("<img id='loading_select' src='/Public/Admin/image/loading.gif' /> ");
                $.ajax({
                    type: 'post',
                    url: '<?php echo U('Comment/delecomment');?>',
                    data: $("#commentsform").serialize(),
                    cache: false,
                    dataType: 'json',
                    error: function (data) {

                        alert("");
                    },
                    success: function (data) {
                        //alert(data);
                        if (data.error == 0) {
                            $("#loading_select").remove();

                            $('input[name="id[]"]').each(function () {
                                if ($(this).prop("checked") == true) {
                                    $(this).parent().parent().remove();
                                }
                            });
                        }

                    }
                });
            }
            return false;
        }
    </script>
</block>
