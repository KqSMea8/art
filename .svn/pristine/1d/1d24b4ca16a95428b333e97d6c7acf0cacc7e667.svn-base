<extend name="Public:base" />
<block name="content">
<link href="/Public/Admin/js/thickbox/thickbox.css" rel="stylesheet">
<style media="screen">
  .androidBox {
    display: none;
    position: fixed;
    width: 100%;
    height: 100%;
    z-index: 99999;
    left: 0;
    top: 0;
    background: rgba(0,0,0,.7);
  }
  .androidFrame {
    width: 600px;
    height: 400px;
    position: fixed;
    left: 50%;
    top: 50%;
    margin-left: -300px;
    margin-top: -200px;
    z-index: 99999;
    background: #fff;
}
</style>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
              <div class="panel-heading">
                android升级补丁
  </div>
                <div class="panel-heading">
                    <div class="row">
                        <div class="col-lg-12"></div><!-- /.col-lg-6 -->
                    </div><!-- /.row -->
                </div>
                <!-- /.panel-heading -->

                <div class="panel-body">
                <!-- <a href="{:U('Attachment/uploadAndroidPatchForm')}" style="color:#F0F; font-size:18px;">点击上传补丁包</a>《《《《 -->
                <span  style="color:#F0F; font-size:18px;">渠道</span>
<table width="100%" class="table table-striped table-bordered table-hover" >
      <thead>
                        <tr>
                            <th></th>
                            <th>补丁地址</th>
                          </tr>
                        </thead>
                        <tbody>
                        <form name="commentsform" id="commentsform" action="" class="form-inline">
                        <foreach name="lists" item="channel" key="k" >
                            <tr>
                                <td><?php
                                    echo htmlspecialchars($channel);
                                    ?></td>
                                <td>&nbsp;<a href="<?php echo U('AppUpgrade/androidPatch');?>?channel={$channel}">管理升级包</a></td>
                            </tr>
                        </foreach>
                        <tr>
                            <th colspan="2"></th>


                        </tr>
                        </form>
                        </tbody>
                    </table>
                    <!-- /.table-responsive -->
                    {$nav}
                    <!-- TODO:page -->
</div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <div id="androidPack" class="androidBox">
      <iframe class="androidFrame" src="{:U('Attachment/uploadAndroidPatchForm')}" width="400px" height="200px"></iframe>
    </div>
</block>

<block name="customJs">
<script language="javascript" type="text/javascript" src="/Public/Admin/js/thickbox/thickbox.js"></script>
    <script>
        $(document).ready(function(){


          $('.upAndroidBtn').click(function(){
            $('#androidPack').show();
          })

          $('#androidPack').click(function(){
            $(this).hide();
          })

                $('#az-id-date-selector').datetimepicker({
                    format:'yyyy-mm-dd',
                    language:  'zh-CN',
                    weekStart: 1,
                    todayBtn:  1,
                    autoclose: 1,
                    todayHighlight: 1,
                    startView: 2,
                    minView: 2,
                    forceParse: 0
                });
            $('#az-id-date-selector2').datetimepicker({
                format:'yyyy-mm-dd',
                language:  'zh-CN',
                weekStart: 1,
                todayBtn:  1,
                autoclose: 1,
                todayHighlight: 1,
                startView: 2,
                minView: 2,
                forceParse: 0
            })





        });
    </script>
    <script type="text/javascript">
        function delete_comment(id,obj) {
            if(confirm("确定删除么?")) {
                $(obj).after("<img id='loading" + id + "' src='/Public/Admin/image/loading.gif' /> ");
                $.ajax({
                    type: 'post',
                    url: '<?php echo U('Article/dele');?>',
                    data: "id=" + id,
                    cache: false,
                    dataType: 'json',
                    error: function (data) {
                        alert("error");
                    },
                    success: function (data) {

                        if (data.error == 0) {
                            $("#loading" + id).remove();
                            $(obj).parent().parent().remove();
                        }

                    }
                });
            }
            return false;
        }
        function check_all(){

            $('input[name="id[]"]').prop("checked", true);
            return false;
        }

        function dis_check_all(){
             $('input[name="id[]"]').prop("checked", false);
            return false;
        }

        function check_fan(){
            $('input[name="id[]"]').each(function(){
                $(this).prop("checked", function(index, attr){
                    return !attr;
                });
            });
            return false;
        }
        function dele_select(obj){

            if(confirm("确定删除么?")) {
                $(obj).after("<img id='loading_select' src='/Public/Admin/image/loading.gif' /> ");
                $.ajax({
                    type: 'post',
                    url: '<?php echo U('Article/dele');?>',
                    data: $("#commentsform").serialize(),
                    cache: false,
                    dataType: 'json',
                    error: function (data) {

                        alert("");
                    },
                    success: function (data) {
                        //alert(data);
                        if (data.error == 0) {
                            $("#loading_select").remove();

                            $('input[name="id[]"]').each(function(){
                                if($(this).prop("checked")==true){
                                    $(this).parent().parent().remove();
                                }
                            });
                        }

                    }
                });
            }
            return false;
        }
    </script>
</block>
